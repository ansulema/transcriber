FROM python:3.12-slim

RUN pip install --no-cache-dir gradio>=5.0.0 requests>=2.31.0 "markitdown[docx]"

WORKDIR /app
COPY frontend/ /app/

ENV BACKEND_URL=http://backend:8001

CMD ["python", "run.py"]
