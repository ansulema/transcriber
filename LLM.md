LLM-часть AI Transcriber
=======================

Модель: Qwen/Qwen3-4B-Instruct-2507 (FP8)
Сервер: vLLM (OpenAI-совместимый API на :11435)
Клиент: OpenAI Python SDK в backend/app/llm.py
Файлы: backend/app/llm.py, backend/app/pipeline.py, backend/main.py

Ресурсы (только vLLM, без Whisper и PyAnnote)
----------------------------------------------

| Компонент | GPU | RAM |
|-----------|-----|-----|
| Веса модели (FP8) | ~4.5 GB | - |
| KV-кэш (8K контекст) | ~2-4 GB | ~1 GB |
| CUDA-графы + оверхед | ~1-2 GB | - |
| vLLM процесс | - | ~3-5 GB |
| **Итого vLLM** | **~17-18 GB** | **~5 GB** |

На практике vLLM резервирует процент от всей GPU-памяти.
С --gpu-memory-utilization 0.55 на 32 GB: ~17 GB под vLLM.
С 0.9 на 80 GB: ~72 GB.

Модель ~4.5 GB, остальное — KV-кэш и CUDA-графы.
Кэш растёт с длиной контекста: 16K ~ в 2-3 раза больше чем 8K.

Оконная суммаризация
--------------------
Включается когда транскрипт > 5000 токенов (tiktoken cl100k_base).
1. Транскрипт разбивается на окна по 5000 токенов с перекрытием 20%
2. Каждое окно суммаризируется промптом SYSTEM_SUMMARIZE:
   "Сделай краткую выжимку: темы, решения, задачи, ответственные, сроки"
3. Все саммари склеиваются и идут в финальный запрос вместе с шаблоном

Заполнение шаблона
------------------
Без шаблона:
  Используется встроенный шаблон с разделами:
  Тема, Дата, Участники, Обсуждённые вопросы, Решения, Задачи (таблица)

С шаблоном (.docx/.md/.txt):
  1. Фронтенд: markitdown читает файл -> текст
  2. Бэкенд: отправляет в LLM вместе с транскриптом
  3. LLM заполняет пустые поля по промпту SYSTEM_MAIN
  4. Результат (markdown) -> pypandoc -> .docx

Промпты
-------
SYSTEM_MAIN: сохранять структуру шаблона, заполнять только пустые поля,
             если данных нет — "не указано", требовать ФИО участников

Подготовка текста для LLM
--------------------------
1. strip_timestamps() — удаляет "Спикер N | MM:SS-MM:SS |" из каждой строки
2. post_process() — убирает слова-паразиты, склеивает строки
3. Если транскрипт не влазит — оконная суммаризация

Запуск vLLM
-----------
conda activate vllm
vllm serve Qwen/Qwen3-4B-Instruct-2507 \
  --port 11435 --host 0.0.0.0 \
  --max-model-len 8192 --quantization fp8 \
  --gpu-memory-utilization 0.55 --trust-remote-code
